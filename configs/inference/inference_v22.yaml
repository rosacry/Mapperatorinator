defaults:
  - base_inference
  - inference
  - osut5: train_v22
  - diffusion: v1
  - _self_

model_path: './test/ckpt_v22'  # Path to trained model
version: 'Mapperatorinator V22'        # Beatmap version
diff_ckpt: 'OliBomby/osu-diffusion-v2'
max_batch_size: 8
